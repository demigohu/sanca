/** Platform default: 90% of available DeFindex yield paid to cycle winner; rest compounds. */
export const PLATFORM_YIELD_SPLIT_BPS = 9000;
export const PLATFORM_YIELD_SPLIT_PERCENT = PLATFORM_YIELD_SPLIT_BPS / 100;
